<% if (theme.scripts) { %>
    <%- js(theme.scripts) %>
<% } %>

<% if (theme.aplayer.script) { %>
    <!-- aplayer: <%- js({ src: theme.aplayer.script, async: true, onload: 'Array.isArray(window.aplayersLite)&&aplayersLite.map(e=>APlayer().init(e))' }) %> -->
<% } %>

<% if (theme.stats.busuanzi.enable) { %>
    <%-
        js({
            src: theme.stats.busuanzi.script || 'https://busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js',
            async: true,
        })
    %>
<% } %>