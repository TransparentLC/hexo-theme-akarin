<% if (page.posts.length > 0) { %>
    <% page.posts.sort('date', -1).limit(10).each(post => { %>
        <%- partial('partials/post-entry', { post }) %>
    <% }); %>
<% } %>

<!-- Paginator in index.ejs -->
<%- partial('partials/paginator') %>